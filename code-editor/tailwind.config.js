/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{js,ts,jsx,tsx}', './index.html'],
  // v4 uses CSS-based configuration via @theme
  // Keep this minimal - most config is in globals.css
}